import { Section } from 'components/Common/Common.styled';
import { default as ServiceList } from '../ServiceList';

const Hero = () => {
	return (
		<Section>
			<ServiceList />
		</Section>
	);
};
export default Hero;
